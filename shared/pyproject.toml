[project]
name = "apollonia-shared"
version = "0.1.0"
description = "Shared utilities for Apollonia services"
requires-python = ">=3.12,<3.13"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
